<Window x:Class="JarLens.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="JarLens" Height="760" Width="1180" MinHeight="660" MinWidth="960"
        Loaded="Window_Loaded">
    <Grid x:Name="Root" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="TitleText" Text="JarLens" FontSize="32" FontWeight="SemiBold" />
                    <TextBlock x:Name="VersionText" Text="v0.0.0" Margin="10,13,0,0" FontSize="13" />
                </StackPanel>
                <TextBlock x:Name="SubtitleText" Text="Static malware triage for Minecraft jars and Java plugins." Margin="0,4,0,0" FontSize="14" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="16,6,0,0">
                <Button Content="Check updates" Width="128" Height="36" Margin="0,0,10,0" Click="CheckUpdates_Click" />
                <Button x:Name="ExportBundleButton" Content="Export bundle" Width="128" Height="36" Margin="0,0,10,0" Click="ExportBundle_Click" IsEnabled="False" />
                <Button Content="Open jar" Width="116" Height="36" Click="OpenJar_Click" />
            </StackPanel>
        </DockPanel>

        <Border x:Name="DropCard" Grid.Row="1" Margin="0,22,0,18" Padding="18" BorderThickness="1" CornerRadius="6"
                AllowDrop="True" Drop="DropZone_Drop" DragOver="DropZone_DragOver">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="320" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock x:Name="DropTitleText" Text="Drop a .jar here or choose one to scan." FontSize="18" FontWeight="SemiBold" />
                    <TextBlock x:Name="TrustText" Text="JarLens does not execute jars, upload files, or make network requests during scanning." Margin="0,7,0,0" />
                    <TextBlock x:Name="SelectedFileText" Text="No file selected" Margin="0,10,0,0" TextWrapping="Wrap" />
                </StackPanel>

                <Border x:Name="RiskCard" Grid.Column="1" BorderThickness="1" CornerRadius="6" Padding="14">
                    <StackPanel>
                        <DockPanel>
                            <TextBlock x:Name="RiskLabelText" Text="Risk" DockPanel.Dock="Left" />
                            <TextBlock x:Name="RiskScoreText" Text="Score: -" DockPanel.Dock="Right" HorizontalAlignment="Right" />
                        </DockPanel>
                        <TextBlock x:Name="RiskText" Text="Not scanned" FontSize="24" FontWeight="SemiBold" Margin="0,5,0,8" />
                        <ProgressBar x:Name="RiskMeter" Minimum="0" Maximum="100" Height="12" Value="0" />
                        <Grid Margin="0,7,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="LowThresholdText" Text="1-29 Low" FontSize="11" />
                            <TextBlock x:Name="MediumThresholdText" Grid.Column="1" Text="30+ Medium" FontSize="11" HorizontalAlignment="Center" />
                            <TextBlock x:Name="HighThresholdText" Grid.Column="2" Text="High rule = High" FontSize="11" HorizontalAlignment="Right" />
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Border x:Name="FindingsCard" Padding="14" BorderThickness="1" CornerRadius="6" Margin="0,0,12,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="FindingsHeaderText" Text="Findings" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <ListBox x:Name="FindingsList" Grid.Row="1" SelectionChanged="FindingsList_SelectionChanged" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,6">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <DockPanel>
                                        <TextBlock Text="{Binding Severity}" FontWeight="SemiBold" DockPanel.Dock="Right" Margin="10,0,0,0" />
                                        <TextBlock Text="{Binding Label}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                    </DockPanel>
                                    <TextBlock Grid.Row="1" Text="{Binding Category}" FontSize="12" Opacity="0.78" />
                                    <TextBlock Grid.Row="2" Text="{Binding Summary}" FontSize="12" Opacity="0.78" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border x:Name="ReportCard" Grid.Column="1" Padding="14" BorderThickness="1" CornerRadius="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock x:Name="DetailTitleText" Text="Report" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock x:Name="DetailSubtitleText" Text="Open a jar to generate a report." Margin="0,4,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <TextBox x:Name="ReportText" IsReadOnly="True" BorderThickness="0" TextWrapping="Wrap" FontFamily="Consolas" FontSize="13"
                                 Text="Open a jar to generate a report." />
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
