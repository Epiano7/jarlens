<Window x:Class="JarLens.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="JarLens" Height="720" Width="1120" MinHeight="620" MinWidth="920"
        Background="#F6F7F9">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <StackPanel>
                <TextBlock Text="JarLens" FontSize="32" FontWeight="SemiBold" Foreground="#15181D" />
                <TextBlock Text="Static malware triage for Minecraft jars and Java plugins." Margin="0,4,0,0" Foreground="#566070" FontSize="14" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="16,6,0,0">
                <Button Content="Check updates" Width="128" Height="36" Margin="0,0,10,0" Click="CheckUpdates_Click" />
                <Button Content="Open jar" Width="116" Height="36" Click="OpenJar_Click" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Margin="0,22,0,18" Padding="18" BorderBrush="#D8DEE8" BorderThickness="1" Background="White" CornerRadius="6"
                AllowDrop="True" Drop="DropZone_Drop" DragOver="DropZone_DragOver">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="260" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Drop a .jar here or choose one to scan." FontSize="18" FontWeight="SemiBold" Foreground="#15181D" />
                    <TextBlock Text="JarLens does not execute jars, upload files, or make network requests by default." Margin="0,7,0,0" Foreground="#566070" />
                    <TextBlock x:Name="SelectedFileText" Text="No file selected" Margin="0,10,0,0" Foreground="#7A8494" TextWrapping="Wrap" />
                </StackPanel>

                <Border Grid.Column="1" Background="#F0F3F7" BorderBrush="#D8DEE8" BorderThickness="1" CornerRadius="6" Padding="14">
                    <StackPanel>
                        <TextBlock Text="Risk" Foreground="#566070" />
                        <TextBlock x:Name="RiskText" Text="Not scanned" FontSize="24" FontWeight="SemiBold" Margin="0,5,0,0" Foreground="#15181D" />
                        <TextBlock x:Name="RiskScoreText" Text="Score: -" Margin="0,5,0,0" Foreground="#566070" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Border Padding="14" Background="White" BorderBrush="#D8DEE8" BorderThickness="1" CornerRadius="6" Margin="0,0,12,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Findings" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <ListBox x:Name="FindingsList" Grid.Row="1" SelectionChanged="FindingsList_SelectionChanged" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,6">
                                    <TextBlock Text="{Binding Label}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                    <TextBlock Text="{Binding Category}" Foreground="#566070" FontSize="12" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="1" Padding="14" Background="White" BorderBrush="#D8DEE8" BorderThickness="1" CornerRadius="6">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="ReportText" IsReadOnly="True" BorderThickness="0" TextWrapping="Wrap" FontFamily="Consolas" FontSize="13"
                             Text="Open a jar to generate a report." />
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
